# php.lang.security.tainted-exec.tainted-exec @ vulnerabilities/exec/source/medium.php:19

**Verdict:** TP · **Confidence:** High (0.0) · **Truth:** real · **Grade:** CORRECT · **Iterations:** 1

## Reasoning

1. The data originates from user‑controlled input via $_REQUEST['ip'] (line 5) – a remote HTTP request source. 2. Flow: line 5 assigns $_REQUEST['ip'] to $target; line 14 passes $target through str_replace() which only removes "&&" and ";"; line 19 or 23 concatenates the (still‑tainted) $target into a string passed to shell_exec(). 3. Sanitization: a blacklist removal of "&&" and ";" only (line 14). This does not cover other shell metacharacters such as "|", "`", "$(…)" etc., so it is insufficient for command injection. 4. The sink is the call to shell_exec() (line 19/23), which launches a command in the underlying OS shell – a classic OS‑command injection sink. 5. No framework or library protection is applied; PHP’s shell_exec() runs the command directly. 6. The code is gated only by a check that a POST field 'Submit' is set (line 3). Any attacker who can submit a POST request to this endpoint (no authentication shown) can trigger the path. 7. If exploited, an attacker can execute arbitrary shell commands on the server (RCE), potentially leading to data theft, privilege escalation, or DoS. 8. The weakest link is the inadequate sanitisation (blacklist that omits many dangerous characters), allowing an attacker to inject shell control characters and break out of the intended "ping" command. All other steps are either direct (source, sink) or lack protective mechanisms. [policy:command_injection entailed]
