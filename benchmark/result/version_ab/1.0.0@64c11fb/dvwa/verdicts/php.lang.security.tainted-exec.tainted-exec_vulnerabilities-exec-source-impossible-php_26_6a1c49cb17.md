# php.lang.security.tainted-exec.tainted-exec @ vulnerabilities/exec/source/impossible.php:26

**Verdict:** FP · **Confidence:** High (0.0) · **Truth:** not-real · **Grade:** CORRECT · **Iterations:** 1

## Reasoning

The user‑controlled value originates from $_REQUEST['ip'] (line 8) – a remote request parameter, establishing PROVEN attacker control. The value is passed through stripslashes (line 9), exploded into octets (line 12), each octet is checked with is_numeric and the array size is verified (line 15). If the checks pass, the octets are reassembled into a dotted string (line 17). This reconstructed string is concatenated into a command string that is executed via shell_exec (line 26), which is an OS command sink. Because the only transformation is numeric validation, the attacker can only supply characters that are numeric (digits, optional sign, decimal point, exponent). These characters do not constitute shell metacharacters, so the command string cannot be broken to inject additional commands. Consequently, the command channel is a plain shell‑command text path, but the validation provides complete coverage against injection, yielding ALL_REACHING_PATHS neutralization. No framework‑level protections are present, and the code can be triggered by any unauthenticated POST request, giving a potential RCE impact if the validation were bypassed. The weakest link is the reliance on is_numeric; however, within the shown code it sufficiently restricts the input, so no bypass path is observed. [policy:command_injection false_positive: command_channel=SHELL_COMMAND_TEXT_PATH_FOUND, neutralization_coverage=ALL_REACHING_PATHS]
